# Data Hooks

The only hooks allowed to import `db` directly, to encapsulate `useLiveQuery` reactive queries.

- Read-only — no write operations
- Naming: `use[Entity]Data.ts`

See [AGENTS.md](../../../AGENTS.md) for rules and examples.
